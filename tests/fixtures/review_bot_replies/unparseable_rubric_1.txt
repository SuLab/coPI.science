{"target":"rubric","suggestion":"**File:** `prompts/rubric/blackbird-rubric.toml`, `[[dimension]]` block with `key = \"scientific_credibility\"`.\n\n**Current text (the `anchors` value):**\n\n```toml\nanchors = \"A credible, *testable* mechanism hypothesis whose key *existing* results are believable — controls, replication, interpretation — with supporting data (genetic, functional, or published). Judge the evidence they have, not evidence the stage hasn't produced: animal rescue is a 5, not the bar for a 4. 1 = the lab cannot say how a key claim was established, or contradictory literature is unacknowledged.\"\n```\n\n**Proposed replacement:**\n\n```toml\nanchors = \"A credible, *testable* mechanism hypothesis whose key *existing* results are believable — controls, replication, interpretation — with supporting data (genetic, functional, or published). Judge the evidence they have, not evidence the stage hasn't produced: animal rescue is a 5, not the bar for a 4. Do not score above 3 until you have asked for, and been given, the replication and quantitation behind the central efficacy claim — how many independent biological replicates, which controls, effect size with dispersion, and whether it is a dose/titration measurement or a single-point demonstration. Where the central claim rests on a single assay format or a single model system, 3 is the ceiling however clean the result looks, and the missing parameters must be named in `rationale`. 1 = the lab cannot say how a key claim was established, or contradictory literature is unacknowledged.\"\n```\n\nSecondary (do not apply in the same edit): the same discipline could be added to `[stage_bar.scientific]`, but the hub — not the specialist — is the party that assigned the 4, so the dimension anchor is the direct target."},"rationale":"The feedback is specifically about the hub scoring `scientific_credibility` above 3 without first extracting replicate counts, controls, and effect sizes for the central efficacy claim. That is a scoring-threshold defect, and the scoring threshold for this dimension lives in the rubric's `scientific_credibility` anchor, which is what the hub reads via `{rubric}`.\n\nThe transcript 